# Peony redesign masters

Selected inputs and final hashes are recorded in `provenance.json`. The six `*-normalized-rgba.png` files are the authoritative final transparent masters. `rare-final-chroma-generated.png` is the selected Full Bloom generation. Other generated images are retained as provenance, not runtime candidates.

The canonical chroma PNGs one directory above are exact-magenta compatibility derivatives. Runtime assets are lossless 1254 × 1254 RGBA WebPs. No soil or shadows are baked into the artwork.

Review and shared integration handoff: `build/peony-redesign/20260905-1906/HANDOFF.md` from the repository root.
